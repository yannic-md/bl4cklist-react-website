export interface GuildFeature {
    src: string;
    alt: string;
    titleKey: string;
    descKey: string;
    animation: string;
}

export interface GuildStatistic {
    end: number;
    suffix?: string;
    icon: string;
    label: string;
}